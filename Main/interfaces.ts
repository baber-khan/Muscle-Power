export interface firebaseAuthUser {
    uid: string,
    name: string,
    Age: number,
    weight: number,
    height: string,
    gender: string
}

export interface excercise {
    id: string,
    title: string
}